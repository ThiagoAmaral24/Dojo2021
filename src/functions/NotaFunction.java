package functions;

import controller.NotasController;


public class NotaFunction {

    /*
    FUNCAO INCOMPLETA
    
    public String SeparaCedulas (Double valor, Double nota) {


        if (nota > 1) {
            int qtd_nota = (int) (valor / nota);
            valor -= (qtd_nota * nota);
            

            return qtd_nota + "notas(s)  de R$ " + nota + "\n";
        } else {
            int qtd_nota = (int) (valor / nota);
            valor -= (qtd_nota * nota);

            return qtd_nota + "moedas(s) de R$ " + nota + "\n";
        }
    }
    FUNCAO INCOMPLETA*/

}


